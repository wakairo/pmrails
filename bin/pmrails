#!/bin/sh
set -eu

# shellcheck disable=SC2016  # Backticks here are intentional typographic markers, not command substitution.
printf '%s\n' \
    'DEPRECATION: `pmrails` has been replaced by `pmrails-run bin/rails`.' \
    'It will be removed in a future release. Please use `pmrails-run bin/rails` instead.' >&2

_PMRAILS_SCRIPT_DIR=$(CDPATH='' cd -- "$(dirname -- "$0")" && pwd)
exec "${_PMRAILS_SCRIPT_DIR}/pmrails-run" bin/rails "$@"
